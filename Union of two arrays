int unionOfTwoArrays(int arr1[], int N, int arr2[], int M)
{
    unordered_map<int, int=""> mp;
    
    for(int i = 0; i < N; i++)
    {
        mp[arr1[i]]++;
    }
    
    for(int i = 0; i < M; i++)
    {
        mp[arr2[i]]++;
    }
    
    return mp.size();
}
