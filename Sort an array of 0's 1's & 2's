void sort012(int a[], int n)
{
    // coode here 
   int l=0,mid=0;
   int high=n-1;
   while(high>=mid){
       if(a[mid]==0){
           swap(a[l++],a[mid++]);
       }
       else if(a[mid]==1){
           mid++;
       }
       else{
           swap(a[mid],a[high--]);
       }
   }
}
