class Solution {
  public:
  int count=0;
    int countSquares(int N) {
        // code here
        int n=sqrt(N);
        float f=sqrt(N);
        
        if(N==1){
            return 0;
        }
        if(n==f){
            return n-1;
        }
        return n;
    }
};
